Drag the .env file into the root of your Barrier Dunes repo.

Do not commit .env to GitHub.
For GitHub Pages deployment, add the same values as GitHub Actions repository variables:
PUBLIC_SUPABASE_URL
PUBLIC_SUPABASE_ANON_KEY
